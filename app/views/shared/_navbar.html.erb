<nav class="navbar navbar-expand-lg bg-body-tertiary px-3">
  <div class="container-fluid">
    <%= link_to root_path, class: 'navbar-brand' do %>
      <%= content_tag :i, '', class: 'fa-solid fa-link' %>
      URL Shortener
    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 d-flex justify-content-center align-items-center">
        <li class="nav-item">
          <% if Current.user%>
            <%= link_to url_creations_path, class: 'nav-link' do %>
              <%= content_tag :i, '', class: 'fa-solid fa-book me-1' %>
              My Links
            <% end %> 
          <% end %>       
        </li>
      </ul>

      <ul class="navbar-nav ms-auto mb-2 mb-lg-0 d-flex justify-content-center align-items-center">
        <% if Current.user %>
          <li class="nav-item me-2">
            <%= link_to Current.user.email, edit_password_path, class: "nav-link" %>
          </li>

          <li class="nav-item">
            <%= link_to "Logout", logout_path, method: :delete, class: "nav-link btn btn-outline-primary px-3" %>
          </li>
        <% end %>
      </ul>
      
    </div>
  </div>
</nav>