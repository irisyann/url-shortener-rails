<div class="container">

    <div class="px-4 py-5 text-center">
        <h1 class="fw-bold">URLs are better shorter.</h1>

        <% if Current.user %>

            <%= render "urls_creations#new" %>

        <% else %>

            <div class="d-grid gap-2 col-7 col-lg-3 mx-auto my-5">
                <%= link_to "Sign Up", sign_up_path, class: "btn btn-primary px-5" %>
                <%= link_to "Sign In", sign_in_path, class: "btn btn-primary px-5" %>
            </div>

        <% end %>
    </div>

</div>  